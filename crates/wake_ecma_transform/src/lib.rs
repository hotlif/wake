//! wake_ecma_transform — 转换管线（TS 擦除/JSX/define）。DESIGN §4.5
//!
//! Phase 0 占位骨架；实现随对应 Phase 填充（见 docs/PLAN.md）。
//! 本文件当前仅保证 crate 可编译、依赖方向正确、`cargo test` 有可运行单元。

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {
        // 占位：确认骨架可编译可测。
    }
}

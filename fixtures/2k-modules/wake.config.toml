# The runner uses `wake bundle --platform browser --format iife --minify`.
# Keep this entry aligned for manual application builds.
browserslist = ["chrome 120", "edge 120", "firefox 121", "safari 17.2", "ios_saf 17.2"]

[html]
entry = "input/entry.js"


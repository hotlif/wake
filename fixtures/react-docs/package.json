{
  "name": "wake-react-docs-fixture",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "cargo run -q --manifest-path ../../Cargo.toml -p wake_cli -- docs dev .",
    "docs:build": "cargo run -q --manifest-path ../../Cargo.toml -p wake_cli -- docs build ."
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "typescript": "^7.0.2"
  }
}

# Third-party notices

The native language server uses `cssparser`, distributed under MPL-2.0, and `tower-lsp-server`,
distributed under MIT or Apache-2.0. The embedded CSS fact snapshot is derived from MDN data through
Microsoft's `vscode-custom-data` project and is attributed under CC BY-SA 2.5.

Build-time JavaScript dependencies and their licenses are recorded in `package-lock.json` and are not
included as runtime language-service implementations.

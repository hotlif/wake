# Wake 仓库工作约定

## 文档、测试、实现顺序

任何会改变可观察行为、公共或跨层契约、诊断、持久化产物、构建或 CI 策略、架构不变量的工作，必须按以下顺序进行：

1. **文档或契约**：先定位最接近的权威材料并明确预期行为。现有文档已经准确表达预期时，引用并复核它，不为流程制造无意义改写；否则先起草或更新相应的用户文档、工程文档、crate rustdoc 或契约。长期架构意向先进入 `proposed` ADR，普通未来事项进入 `engineering/ROADMAP.md`；`proposed` ADR 不得作为当前边界或机器策略依据。描述当前状态的文档可以在工作树中先行起草，但只能随对应测试和实现组成完整变更后提交或交付，不得提前发布为已支持能力。
2. **测试证据**：实现前新增或调整能表达预期的最小测试或 fixture，并确认它因目标缺口而失败。已有精确失败测试时先复现，不重复造测试。
3. **最小实现**：只在文档和测试明确后修改实现；先让聚焦测试转绿，再运行与风险匹配的门禁，并使最终文档、测试与实现保持一致。

行为保持重构先运行现有聚焦测试建立基线，只有存在覆盖缺口时才补测试。纯文档、纯测试、只读审查、格式或版本同步不得制造虚假失败测试。行为缺陷无法稳定复现时，先构造最小复现；仍不能复现则在修改实现前报告阻塞。

具体测试入口和风险门禁见 `engineering/TESTING.md`。实现期间允许工作树短暂处于文档领先或测试失败状态，但不得提交或交付文档、测试与实现不一致的结果。
